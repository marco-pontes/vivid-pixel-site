// Common constants for API endpoints and query keys
export const API_ENDPOINTS = {
	SEND_CONTACT: `/api/contact/`,
} as const;

export const QUERY_KEYS = {
	SEND_CONTACT: "send_contact" as const,
} as const;
